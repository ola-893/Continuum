# ============================================================================
# AIP AGENT PYTHON MICROSERVICE - ENVIRONMENT CONFIGURATION
# ============================================================================
# This file contains all required environment variables for the Python
# microservice that wraps the AIP Agent SDK and provides REST endpoints
# for the Node.js backend.
#
# SECURITY WARNING: Never commit this file with real values to git!
# Keep your MEMBASE_SECRET_KEY secure at all times.
# ============================================================================

# ----------------------------------------------------------------------------
# BLOCKCHAIN IDENTITY (REQUIRED)
# ----------------------------------------------------------------------------
# These variables establish the blockchain identity for your AI agent.
# The agent will be registered on-chain using the Membase smart contract.

# Your BNB Chain wallet address that will own the agent
# Format: 0x followed by 40 hexadecimal characters
# Example: 0x1234567890abcdef1234567890abcdef12345678
# Get testnet BNB from: https://testnet.bnbchain.org/faucet-smart
MEMBASE_ACCOUNT=0x1234567890abcdef1234567890abcdef12345678

# Your wallet private key (KEEP SECRET - never commit to git!)
# This is used to sign blockchain transactions for agent registration
# Format: 64 hexadecimal characters (with or without 0x prefix)
MEMBASE_SECRET_KEY=your_private_key_here

# Unique agent identifier (e.g., "continuum_agent_001")
# This ID will be registered on-chain and linked to your wallet address
# Must be unique across all agents in the Membase system
# Format: alphanumeric string with underscores allowed
MEMBASE_ID=continuum_agent_001

# ----------------------------------------------------------------------------
# NETWORK CONFIGURATION (REQUIRED)
# ----------------------------------------------------------------------------

# Blockchain network to use
# Options: bsc-testnet (for development), bsc-mainnet (for production)
# Default: bsc-testnet
MEMBASE_NETWORK=bsc-testnet

# BSC RPC endpoint for blockchain transactions
# Testnet: https://bsc-testnet-rpc.publicnode.com
# Mainnet: https://bsc-dataseed.binance.org
BSC_RPC_URL=https://bsc-testnet-rpc.publicnode.com

# Membase smart contract address (DO NOT CHANGE)
# This is the on-chain registry that links agent IDs to wallet addresses
# Testnet: 0x100E3F8c5285df46A8B9edF6b38B8f90F1C32B7b
MEMBASE_CONTRACT_ADDRESS=0x100E3F8c5285df46A8B9edF6b38B8f90F1C32B7b

# Memory Hub gRPC address (provided by Unibase)
# This is the decentralized memory server for agent state persistence
# Format: host:port
MEMORY_HUB_ADDRESS=54.169.29.193:8081

# ----------------------------------------------------------------------------
# LLM INTEGRATION (REQUIRED)
# ----------------------------------------------------------------------------
# Configure the LLM provider for agent intelligence and responses

# OpenAI API key for LLM-powered responses
# Get your key from: https://platform.openai.com/api-keys
# Format: sk- followed by alphanumeric characters
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI model to use (optional)
# Default: gpt-4-turbo-preview
# Options: gpt-4-turbo-preview, gpt-4, gpt-3.5-turbo
OPENAI_MODEL=gpt-4-turbo-preview

# Alternative: ChainGPT API key (if using ChainGPT instead of OpenAI)
# CHAINGPT_API_KEY=your-chaingpt-api-key-here
# CHAINGPT_MODEL=chaingpt-v2

# ----------------------------------------------------------------------------
# SERVICE CONFIGURATION
# ----------------------------------------------------------------------------

# Port for the Python microservice
# The Node.js backend will connect to this port
# Default: 5000
PORT=5000

# Flask environment
# Options: development (verbose logging), production (optimized)
# Default: development
FLASK_ENV=development

# Logging level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
LOG_LEVEL=INFO

# Enable debug mode (DO NOT USE IN PRODUCTION)
# Options: true, false
# Default: false
DEBUG=false

# ----------------------------------------------------------------------------
# CORS CONFIGURATION (OPTIONAL)
# ----------------------------------------------------------------------------
# Configure Cross-Origin Resource Sharing for API access

# Comma-separated list of allowed origins
# Example: http://localhost:3000,http://localhost:3001,https://yourdomain.com
# Leave empty to allow all origins (not recommended for production)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ----------------------------------------------------------------------------
# ADVANCED CONFIGURATION (OPTIONAL)
# ----------------------------------------------------------------------------

# Request timeout for blockchain operations (in seconds)
# Default: 30
BLOCKCHAIN_TIMEOUT=30

# Request timeout for Memory Hub operations (in seconds)
# Default: 10
MEMORY_HUB_TIMEOUT=10

# Request timeout for LLM API calls (in seconds)
# Default: 30
LLM_TIMEOUT=30

# Maximum retries for failed operations
# Default: 3
MAX_RETRIES=3

# Gas price multiplier for blockchain transactions (1.0 = normal, 1.5 = 50% higher)
# Default: 1.0
GAS_PRICE_MULTIPLIER=1.0

# Enable agent state caching (improves performance)
# Options: true, false
# Default: true
ENABLE_CACHING=true

# Cache TTL in seconds
# Default: 300 (5 minutes)
CACHE_TTL=300
