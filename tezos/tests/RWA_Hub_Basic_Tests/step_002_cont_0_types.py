import smartpy as sp

tstorage = sp.record(active_rentals = sp.big_map(sp.address, sp.nat), admin = sp.address, asset_yield_protocol = sp.address, compliance_guard = sp.address, streaming_protocol = sp.address, token_registry = sp.address).layout(("active_rentals", ("admin", ("asset_yield_protocol", ("compliance_guard", ("streaming_protocol", "token_registry"))))))
tparameter = sp.variant(batch_whitelist = sp.record(asset_types = sp.set(sp.nat), users = sp.list(sp.address)).layout(("asset_types", "users")), compliant_claim_yield = sp.record(nft_token_id = sp.nat, token_address = sp.address).layout(("nft_token_id", "token_address")), compliant_flash_advance = sp.record(amount_requested = sp.nat, nft_token_id = sp.nat, token_address = sp.address).layout(("amount_requested", ("nft_token_id", "token_address"))), create_commodities_stream = sp.record(duration = sp.nat, metadata_uri = (existential type), token_address = sp.address, total_yield = sp.nat, yield_token_address = sp.address, yield_token_id = sp.nat).layout(("duration", ("metadata_uri", ("token_address", ("total_yield", ("yield_token_address", "yield_token_id")))))), create_compliant_rwa_stream = sp.record(asset_type = (existential type), duration = sp.nat, metadata_uri = (existential type), token_address = sp.address, total_yield = sp.nat, yield_token_address = sp.address, yield_token_id = sp.nat).layout(("asset_type", ("duration", ("metadata_uri", ("token_address", ("total_yield", ("yield_token_address", "yield_token_id"))))))), create_real_estate_stream = sp.record(duration = sp.nat, metadata_uri = (existential type), token_address = sp.address, total_yield = sp.nat, yield_token_address = sp.address, yield_token_id = sp.nat).layout(("duration", ("metadata_uri", ("token_address", ("total_yield", ("yield_token_address", "yield_token_id")))))), create_securities_stream = sp.record(duration = sp.nat, metadata_uri = (existential type), token_address = sp.address, total_yield = sp.nat, yield_token_address = sp.address, yield_token_id = sp.nat).layout(("duration", ("metadata_uri", ("token_address", ("total_yield", ("yield_token_address", "yield_token_id")))))), emergency_freeze = sp.record(reason = sp.string, stream_id = sp.nat).layout(("reason", "stream_id")), register_rental_stream = sp.record(stream_id = sp.nat, token_address = sp.address).layout(("stream_id", "token_address")), register_token_after_stream = sp.record(asset_type = sp.nat, metadata_uri = sp.string, stream_id = sp.nat, token_address = sp.address).layout(("asset_type", ("metadata_uri", ("stream_id", "token_address")))), stream_rent_to_asset = sp.record(duration = sp.int, nft_token_id = (existential type), payment_amount = sp.int, payment_token_address = (existential type), payment_token_id = (existential type), token_address = (existential type)).layout(("duration", ("nft_token_id", ("payment_amount", ("payment_token_address", ("payment_token_id", "token_address"))))))).layout(((("batch_whitelist", "compliant_claim_yield"), ("compliant_flash_advance", ("create_commodities_stream", "create_compliant_rwa_stream"))), (("create_real_estate_stream", ("create_securities_stream", "emergency_freeze")), ("register_rental_stream", ("register_token_after_stream", "stream_rent_to_asset")))))
tprivates = { }
tviews = { "check_access_status": (sp.record(stream_id = sp.nat, token_address = sp.address).layout(("stream_id", "token_address")), sp.bool), "get_active_rental": (sp.address, sp.nat), "can_participate": (sp.record(asset_type = sp.nat, user = sp.address).layout(("asset_type", "user")), sp.bool), "get_stream_status": ((existential type), sp.record(is_frozen = sp.bool, stream_info = sp.record(amount_withdrawn = sp.nat, flow_rate = sp.nat, recipient = sp.address, sender = sp.address, start_time = sp.timestamp, status = sp.nat, stop_time = sp.timestamp, token_address = sp.address, token_id = sp.nat, total_amount = sp.nat).layout(("amount_withdrawn", ("flow_rate", ("recipient", ("sender", ("start_time", ("status", ("stop_time", ("token_address", ("token_id", "total_amount"))))))))))).layout(("is_frozen", "stream_info"))), "get_user_compliance_status": (sp.record(asset_type = sp.nat, user = sp.address).layout(("asset_type", "user")), sp.record(asset_type = sp.nat, has_valid_kyc = sp.bool, is_authorized = sp.bool).layout(("asset_type", ("has_valid_kyc", "is_authorized")))) }
