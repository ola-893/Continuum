# Example environment variables for the Continuum frontend

# ============================================
# Backend API Configuration
# ============================================

# Backend API URL
# URL of the Node.js backend service that communicates with the Python microservice
# Default: http://localhost:3001 (for local development)
# Production: Update to your deployed backend URL
VITE_API_URL="http://localhost:3001"

# Backend Request Timeout (in milliseconds)
# Maximum time to wait for backend API responses
# Default: 30000 (30 seconds)
# Range: 1000-300000 (1 second to 5 minutes)
VITE_API_TIMEOUT="30000"

# Maximum Retry Attempts (OPTIONAL)
# Number of times to retry failed API requests
# Default: 3
# Range: 0-10
VITE_API_MAX_RETRIES="3"

# Retry Delay (in milliseconds) (OPTIONAL)
# Base delay for exponential backoff retry logic
# Default: 1000 (1 second)
# Range: 100-10000 (100ms to 10 seconds)
VITE_API_RETRY_DELAY="1000"

# ============================================
# Blockchain Configuration
# ============================================

# Aptos Network configuration (e.g., testnet, devnet, mainnet)
VITE_APTOS_NETWORK="testnet"

# ============================================
# IPFS Configuration
# ============================================

# Pinata JWT for IPFS uploads (required for minting new assets with IPFS metadata)
# Get your JWT from https://app.pinata.cloud/developers/api-keys
VITE_PINATA_JWT="your_pinata_jwt_here"

# Public IPFS Gateway URL
# This is used to fetch and display IPFS metadata. Ensure it ends with a slash.
VITE_IPFS_GATEWAY="https://ipfs.io/ipfs/"

# ============================================
# Required Environment Variables
# ============================================
# 
# VITE_API_URL: Backend API URL (REQUIRED)
#   - Must point to the Node.js backend service
#   - The backend communicates with the Python microservice for AIP Agent operations
#   - Example: http://localhost:3001 (local) or https://api.continuum.example.com (production)
#
# VITE_API_TIMEOUT: Backend request timeout in milliseconds (OPTIONAL)
#   - Default: 30000 (30 seconds)
#   - Range: 1000-300000 (1 second to 5 minutes)
#   - Increase if backend operations take longer (e.g., blockchain transactions)
#
# VITE_API_MAX_RETRIES: Maximum retry attempts for failed requests (OPTIONAL)
#   - Default: 3
#   - Range: 0-10
#   - Set to 0 to disable retries
#
# VITE_API_RETRY_DELAY: Base delay for exponential backoff in milliseconds (OPTIONAL)
#   - Default: 1000 (1 second)
#   - Range: 100-10000 (100ms to 10 seconds)
#   - Actual delay doubles with each retry attempt
#
# VITE_APTOS_NETWORK: Aptos blockchain network (REQUIRED)
#   - Options: testnet, devnet, mainnet
#   - Default: testnet
#
# VITE_PINATA_JWT: Pinata API JWT for IPFS uploads (OPTIONAL)
#   - Only required if using IPFS metadata uploads
#   - Get from: https://app.pinata.cloud/developers/api-keys
#
# VITE_IPFS_GATEWAY: Public IPFS gateway URL (OPTIONAL)
#   - Default: https://ipfs.io/ipfs/
#   - Used to fetch and display IPFS metadata
#
# ============================================
# Notes
# ============================================
#
# - No Unibase SDK API keys are required (authentication is wallet-based)
# - The frontend communicates with the Node.js backend via REST API
# - The Node.js backend handles all AIP Agent operations via the Python microservice
# - All blockchain operations use wallet-based authentication (MEMBASE_ACCOUNT + MEMBASE_SECRET_KEY)
#   configured in the backend services, not the frontend
#
