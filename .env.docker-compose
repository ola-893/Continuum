# Docker Compose Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# PYTHON MICROSERVICE CONFIGURATION (Required for AIP Agent)
# ============================================================================

# Blockchain Identity (REQUIRED)
# Your BNB Chain wallet address that will own the agent
MEMBASE_ACCOUNT=0x1234567890abcdef1234567890abcdef12345678

# Your wallet private key (KEEP SECRET - never commit to git)
# This is used to sign blockchain transactions for agent registration
MEMBASE_SECRET_KEY=your_private_key_here

# Unique agent identifier (e.g., "continuum_agent_001")
# This will be registered on-chain and linked to your wallet
MEMBASE_ID=continuum_agent_001

# Network Configuration
# Options: bsc-testnet, bsc-mainnet
MEMBASE_NETWORK=bsc-testnet

# Memory Hub gRPC address (provided by Unibase)
# This is the decentralized memory storage server
MEMORY_HUB_ADDRESS=54.169.29.193:8081

# LLM Integration (for agent intelligence)
# OpenAI API key for LLM-powered responses
OPENAI_API_KEY=sk-your-openai-api-key-here

# Alternative: ChainGPT API key
CHAINGPT_API_KEY=

# Python Service Configuration
FLASK_ENV=development
LOG_LEVEL=INFO

# ============================================================================
# NODE.JS BACKEND CONFIGURATION
# ============================================================================

# AIP Agent Configuration
# Timeout for requests to Python service (milliseconds)
AIP_TIMEOUT=30000
# Maximum number of retry attempts
AIP_MAX_RETRIES=3

# BitAgent Configuration (legacy - optional)
BITAGENT_API_KEY=your_bitagent_api_key_here
BITAGENT_NETWORK=bnb-testnet

# BSC Configuration
BSC_TESTNET_RPC_URL=https://bsc-testnet.publicnode.com

# Node.js Environment
NODE_ENV=production

# ============================================================================
# NOTES
# ============================================================================
# 
# 1. MEMBASE_ACCOUNT and MEMBASE_SECRET_KEY are REQUIRED for agent registration
# 2. You need BNB testnet tokens for gas fees (get from https://testnet.bnbchain.org/faucet-smart)
# 3. OPENAI_API_KEY or CHAINGPT_API_KEY is required for agent intelligence
# 4. The Python service will be available at http://localhost:5000
# 5. The Node.js backend will be available at http://localhost:3001
# 6. Services communicate internally via Docker network (python-service:5000)
#
# To start services:
#   docker-compose up -d
#
# To view logs:
#   docker-compose logs -f
#
# To stop services:
#   docker-compose down
#
